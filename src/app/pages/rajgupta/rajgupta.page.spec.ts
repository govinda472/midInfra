import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RajguptaPage } from './rajgupta.page';

describe('RajguptaPage', () => {
  let component: RajguptaPage;
  let fixture: ComponentFixture<RajguptaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RajguptaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
